// Using POP Method

// initializing the list of the games
let Games = ["maskgun", "GTA5", "CS", "COD", "PuBg"];

// Remove an items from the begineer of the array(POP)
let last = Games.pop();

// Remove an item from the beginner of the array

let first = Games.shift();

// Printing the output
 
console.log(first);
console.log(last);
console.log(Games.length);